<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container">
    <%=link_to "Photogram (Industrial)", root_path, class:"navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
  
        
        <%if user_signed_in?%>
          <li class="nav-item">
            
            <%=link_to feed_path(current_user.username), class:"nav-link" do%>
              <i class="fas fa-users fa-fw me-2"></i>Feed
            <%end%>
          </li>
          <li class="nav-item">
              <%=link_to discover_path(current_user.username), class:"nav-link" do%>
                <i class="fas fa-compass fa-fw me-2"></i>Discover
              <%end%>
          </li>
          <li class="nav-item">
              <%=link_to new_photo_path, class:"nav-link" do%>
                <i class="fas fa-plus-circle fa-fw me-2"></i>Add post
              <%end%>
          </li>
          <li class="nav-item">
            <%=link_to user_path(current_user.username), class:"nav-link" do%>
              <%=image_tag current_user.avatar_image_url, class: "rounded-circle me-2", width: "24px", height: "24px"%>Your profile
            <%end%>
          </li>
          <li class="nav-item">
            <%=link_to "Edit #{current_user.username}", edit_user_registration_path, class:"nav-link"%>
          </li>
          <li class="nav-item">
            <%=link_to "Sign out", destroy_user_session_path, method: :delete, class:"nav-link"%>
          </li>
        <%else%>
          <li class="nav-item">
            <%=link_to "Sign up", new_user_registration_path, class:"nav-link"%>
          </li>
          <li class="nav-item">
            <%=link_to "Sign in", new_user_session_path, class:"nav-link"%>
          </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>
